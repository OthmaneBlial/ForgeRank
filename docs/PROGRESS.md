# Implementation progress

Last audited: 2026-08-16

Estimated completion against the complete 150-section specification: **98%**. This is a weighted implementation estimate, not a claim that 98% of long-running historical data has already accumulated.

## Verified in the current worktree

- Strict Next.js/React/TypeScript project with repository-wide Prettier check, production build, lint, typecheck, Vitest, and Playwright configuration.
- 22-table PostgreSQL schema plus migrations for repositories, repository/developer/ranking/ecosystem snapshots, aliases, Git analyses, privacy-safe contributors, developers, auditable profile controls, request-policy state, persistent privacy-hashed intake limits, topics, collections, jobs/failures, source documents, and system state.
- 62 real repository identifiers, 10 developer identifiers, and five collection definitions; seeds contain no analytics.
- Policy-aware public HTML fetcher, versioned repository/profile fixture parsers, append-only persistence, source hashes/provenance, scoring, ranking, alias transfer handling, and queue worker.
- Shallow/blobless Git inspection with a real `solidjs/solid` run proving language, activity, author, quality, and technology enrichment.
- Real local observations for ten repositories, including a verified transfer from `facebook/react` to `react/react`, plus a public-profile/owned-project developer scoring run for `sharkdp`.
- Deterministic topic classification, versioned six-dimension repository similarity, technology ecosystem/detail pages, responsive developer portfolio/score views, privacy-safe JSON/CSV repository export, and dynamic repository/developer social cards. Similarity v2 uses every specified deterministic input—language, topics, detected technologies, bounded description keywords, and curated-collection overlap—plus a small lifecycle signal, without embeddings or external AI.
- Developer detail now exposes owned-project activity, language breadth, project-level multi-author collaboration with explicit denominators, confirmed-link-only contributions, and a provenance timeline without relabeling Git authors as public accounts. Collaboration evidence feeds the transparent developer score.
- Developer leaderboards now provide nine distinct shareable evidence categories: Overall, Impact, Consistency, Collaboration, Builders, Maintainers, Rising, Most Active, and Veterans. Public-ecosystem, public-location, bounded-activity, and non-exclusive archetype filters compose in the URL; every category has an explicit eligibility rule, coverage denominator, methodology, and missing-evidence state. Account age remains withheld rather than inferred, and Veterans is labeled as observed project longevity.
- Local operator controls can hide/restore a developer profile and set/hide/revert public profile fields; each action requires a reason, appends an audit event, preserves source/repository history, and is reviewable through the CLI.
- Eight explicit snapshot-backed Trending, Rising, Breakout, Most Improved, Hidden Gems, Established, Most Active, and Cooling Giants definitions with 1/7/30-day URLs, evidence cards, confidence gates, counter-anomaly rejection, and honest insufficient-history states. Most Improved requires retained scored snapshots; Cooling Giants requires established impact and measured deceleration and uses neutral language rather than predicting abandonment.
- Git analysis v2 now measures a fully observed prior quiet interval from bounded two-year/depth-limited commit history. The nullable evidence is persisted and only produces a Revived lifecycle when the existing ≥180-day quiet-gap, recent-commit, and sustained-active-week rules all pass; the PostgreSQL audit proves the complete evidence-to-lifecycle path.
- Git analysis v3 adds deterministic README structure evidence without storing or rendering raw README content: the shallowest canonical path, exact observed blob size, bounded line/heading/badge/installation/documentation-link signals, up to 20 sanitized headings, and an explicit unavailable state above the 512 KiB inspection cap. Repository UI, methodology, PostgreSQL migration, and JSON export expose the same versioned evidence without assigning a documentation-quality grade.
- Git analysis v4 adds technology-detection-v2 as a real pluggable detector registry across every manifest named in the specification: package.json, pyproject.toml, requirements.txt, Cargo.toml, go.mod, pom.xml, Gradle, Gemfile, composer.json, pubspec.yaml, Dockerfiles, and Docker Compose. Node, Python, Rust, Go, JVM, Ruby, PHP, Dart/Flutter, container, CI, and package-manager detections retain explicit evidence and confidence. Repository-quality-signals-v2 separately covers all eleven required structures—README, license, contributing, code of conduct, security, tests, CI, Docker, release automation, dependency management, and dedicated documentation—without changing repository-v1 scoring semantics or making quality/security claims. The repository UI, methodology, PostgreSQL audit, and JSON export v3 expose the same versions and evidence.
- Ranking runs append versioned primary-language ecosystem snapshots with indexed/scored/Git-analyzed denominators, stars, activity, and score aggregates; language pages render only this retained history and visibly withhold trends before two observations exist.
- Low-priority snapshot maintenance is dry-run-first in the CLI, automatic in the continuous worker, and deterministically retains full resolution for 90 days, daily representatives through one year, then weekly representatives by entity. It never runs in a public request.
- Repository score v1 now persists exactly one structured evidence reason for every dimension in the current projection and latest snapshot. The detail page exposes each reason's positive/neutral/caution/missing status, raw observed inputs, confidence multiplier, score version, and evidence timestamp without reconstructing explanations for legacy snapshots.
- Repository details now query bounded append-only global ranking history, state the exact current scored cohort, plot accessible completed-run positions, and calculate 24-hour, 7-day, and 30-day movement only from a real baseline at or before each requested window. Missing runs remain visibly unavailable rather than interpolated.
- Repository details now expose versioned human-readable ForgeRank Signals for maintenance cadence, lifecycle, Git-author distribution, snapshot-backed momentum, deterministic repository structures, and visible Git tags. Contributor concentration is explicitly an estimate rather than an exact bus factor; missing inputs produce no signal, and every card avoids quality, security, and maintainer-intent claims.
- Repository details now expose an accessible, responsive repository-events-v1 project timeline derived from bounded retained snapshots, comparable completed ranking runs, and Git analyses. Explicit tested rules cover observed-star and indexed-rank crossings, same-version momentum increases, resumed activity, dormancy transitions, and increased Git tag counts. First observations never backfill milestones, anomalous/version-mismatched pairs are rejected, tags are not automatically labeled releases, exact external times are not reconstructed, and JSON export v2 includes the same evidence records.
- The repository leaderboard now implements the specification's complete evidence workbench: ten ranking modes, six real snapshot periods, dynamic language, observed-star, repository-age, lifecycle, original/fork, and search filters compose in shareable URLs. Server-side pagination and sortable columns expose reach, boundary-backed growth, bounded Git activity, lifecycle, age, score, exact filtered/scored/growth/Git-analysis denominators, and mobile evidence cards without inventing unavailable contributor identities. Dedicated database indexes support its principal filters and sorts.
- WCAG A/AA automation now audits the homepage, repository leaderboard/detail, ecosystem comparison, developer leaderboard/detail, daily/weekly reports, and command palette in light and dark themes at desktop and mobile sizes. Shared contrast/touch-target defects were corrected; skip-link focus, modal focus trapping/restoration, reduced motion, accessible table naming, and mobile search labeling have explicit browser coverage.
- A separate optimized-production performance gate rebuilds the isolated network-free fixture, then enforces TTFB, LCP, CLS, document/load timing, transferred/decoded JavaScript, DOM-size, and long-task budgets on desktop/mobile homepage, filtered repository leaderboard, discovery modes, and repository detail routes. All eight profiles pass; the reproducible local baseline and its field-data caveat are documented in `PERFORMANCE.md`.
- Daily and weekly intelligence reports now use deterministic domain selectors and explicit observation windows for repository growth, breakouts, rank changes, newly observed active non-forks, language-ecosystem movement, revivals, completed rankings, and confirmed-profile developer spotlights. Both expose indexed/scored/ranked/history denominators and render evidence-specific empty states when a baseline is too young.
- The operator dashboard now has a fail-closed first-party access boundary: exact loopback-only development opt-in, explicitly enabled production HTTP Basic credentials, constant-time comparison, proxy and protected-read checks, no-store/noindex responses, and live production-server verification of its 404/401/200 outcomes.
- Deployment packaging now separates a minimal Next.js standalone web image from the CLI/worker operations image, runs both as an unprivileged user under `tini`, retains the seed fixtures the documented container command needs, and separates PostgreSQL, Git, and HTTP cache volumes. A clean GitHub runner has built both images, migrated and seeded PostgreSQL, reached database readiness, started the standalone web image, and successfully requested the public coverage page. Drizzle migration metadata and the coverage route are explicitly tracked, preventing local-only files from masking clean-checkout failures.
- A clean real-PostgreSQL integration audit now proves the production driver, all migrations and their idempotency, 22 public tables, `pg_trgm`, exact seed counts, a filtered Momentum Matrix topic join, rich repository-leaderboard query with real snapshot growth, persisted quiet-gap evidence through actual Revived classification and derived timeline events, `FOR UPDATE SKIP LOCKED` queue claiming, database-backed rate limiting, and worker-heartbeat persistence. It can create and remove an isolated local cluster or target a disposable `DATABASE_URL`, and has a dedicated PostgreSQL 17 CI service definition.
- Public no-store liveness and database-readiness endpoints now separate process health from dependency health. Workers publish database-backed heartbeats with fresh/stale/stopping/missing states, expose a strict operator CLI probe, remove their record on graceful shutdown, and drive the Compose health check. The public coverage page now reports scoring, Git-analysis, snapshot, 24-hour refresh, overdue-refresh, and last-ranking freshness without exposing queue or acquisition internals.
- E2E no longer depends on the accumulated development database: each run removes and rebuilds one tightly scoped test-only PGlite directory, loads sanitized deterministic evidence without network access, and launches Next.js against that isolated database. This activated and fixed a previously hidden nested-interactive Momentum Matrix defect. CI now runs both desktop and mobile projects against the same reproducible fixture.
- The signature Momentum Matrix now has composable shareable language, deterministic-topic, observed repository-age, and minimum-star filters; a logarithmic observed-popularity axis; fixed 0–100 momentum; real snapshot-window growth with confidence/span disclosure; bounded index/eligibility/growth denominators; keyboard-accessible repository links; native hover details; and compact touch-readable evidence cards. Filtered desktop/mobile visual artifacts were inspected after correcting chart-overlay containment.
- Fifteen data-heavy route segments now stream a shared accessible skeleton while keeping the site shell interactive. An app error boundary offers explicit retry without replacing failed evidence, a self-contained global boundary covers root-layout failure, and the custom recoverable 404 was browser-verified to retain HTTP 404 semantics. Skeletons are hidden from assistive technology except for one busy-state announcement and honor reduced motion.
- Product routes listed in the README, responsive mobile leaderboard cards, light/dark theme, keyboard search, PWA shell, badges, sitemap, and local watchlists/comparisons.
- Indexed search now uses PostgreSQL full-text ranking and `pg_trgm` fuzzy indexes, understands repositories/owners/descriptions, confirmed developers, languages, technologies, topics, and collections, returns real popular suggestions, and supports Arrow/Home/End/Enter command-palette navigation.
- Two-to-five repository comparisons expose current counts, fixed-scale score dimensions, bounded Git activity, freshness, confidence, technologies, and only growth calculated inside a shared valid snapshot window; inaccessible or insufficient evidence remains visibly unavailable.
- Shareable two-to-five language-ecosystem comparisons aggregate only ForgeRank's observed primary-language cohorts, expose scored/Git-analyzed denominators, keep published score scales distinct from relative raw-count bars, and link back to the strongest underlying projects.
- Persistent shared hourly/daily crawl budgets and host circuits, stale worker-lock recovery, quota-bound safe Git-cache eviction, and local operator visibility.
- Deterministic hot/active/normal/cold refresh tiers, staleness/interest/collection/momentum priority scoring, an automatic worker scheduler, queue promotion/deduplication, CLI scheduling/refresh commands, and truthful repository freshness/request UX.
- Protected operations visibility covers refresh tiers and due work, recent jobs, fetch success, parser/circuit state, database and Git-cache size, request budgets, and the last ranking run.
- Dynamic sitemap entries cover observed repositories, confirmed developers, languages, technologies, topics, and collections.
- 139 unit tests pass across 34 files, including repository/profile parsers, repository-leaderboard filter normalization and bounded growth windows, deterministic README structure extraction, versioned retained-evidence repository events, bounded Git quiet-gap measurement, developer activity/correction policy, evidence-gated developer leaderboard categories/filters, structured repository score reasons, human-readable repository signals, ranking-history windows, Momentum Matrix filters and real window-baseline selection, accessible route loading state, snapshot retention, worker-heartbeat states, CSV safety, URL/SSRF boundary, scoring, eight distinct trending/discovery scenarios, deterministic report selection, comparable growth/ecosystem aggregation, lifecycle, refresh policy, fuzzy matching, operator authentication, proxy behavior, six-dimension similarity and bounded description tokenization, Git-cache path safety, the pluggable manifest detector registry, all eleven repository structure signals, and freshness/size formatting.
- Zero-API architectural guard passed across 190 runtime and dependency files.
- The production dependency audit reports no known vulnerabilities and now runs as an explicit script, CI quality step, and pull-request evidence item.
- Production Next build passed and populated desktop/mobile screenshots, including dedicated repository timeline, README-analysis, and repository-structure artifacts, were inspected.

## Known incomplete requirements

- Long-running repository snapshot cadence has not yet accumulated real 7/30/90-day windows, so momentum-heavy views correctly remain limited even though ecosystem persistence and retention are now operational.
- Developer ingestion, evidence-specific leaderboards, and portfolio-level collaboration now work for confirmed public profiles and explicitly non-fork owned repositories. The current dataset has no independently confirmed Git-author/account links, no dependable public account-age field, and only one enriched developer portfolio; broader coverage must come from legitimate observations rather than inference.
- Chromium E2E passes 82/82 isolated desktop/mobile runs across 41 core and accessibility checks, including explicit liveness/readiness behavior, public coverage health, truthful 404 status/recovery, completed-snapshot rank history, deterministic ForgeRank Signals, retained-evidence repository timelines and JSON export v3, versioned technology/structure and similarity evidence, the filtered Momentum Matrix, methodology and discovery-mode accessibility, hydration-safe fuzzy keyboard search, rich shareable repository filters and sortable evidence, eight distinct shareable trend modes, developer ranking modes, deterministic daily/weekly reports, missing-data detail behavior, persisted dimension-level score explanations, policy-governed refresh requests, rich repository/ecosystem comparisons, developer identity boundaries, hydration-clean ecosystem history, queued submissions, the mobile card layout, theme contrast, keyboard focus, and reduced motion. A separate 8/8 deterministic standalone-production performance audit also passes.
- The complete 32-point initial milestone has not yet been proven; the goal remains active.
