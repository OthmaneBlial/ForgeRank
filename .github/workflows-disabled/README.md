# Disabled workflows

GitHub Actions is intentionally paused while ForgeRank is being reworked.

The previous CI workflow is preserved here as `ci.yml.disabled`. To re-enable it,
move it back to `.github/workflows/ci.yml`, run the full verification suite
locally, and commit the move.
